# Cleared screenshot assets

The following **original local application captures** were generated on 2026-09-06 by `tests/browser/product.spec.ts` against the real local Cloudflare Workers/D1 runtime. The fictional fencing request was newly saved/published in synthetic workspaces. Independent customer contexts recorded real local database responses. These are not deployed-app screenshots and do not represent actual customer consent. The test workspaces were explicitly deleted after verification.

| File | Captured state / privacy review |
| --- | --- |
| `extra-work-approval-composer.png` | Fictional entered draft before first save; owner note closed and no capability displayed |
| `extra-work-approval-preview.png` | Read-only customer preview in the actual dialog; no private notes or customer decision control |
| `extra-work-approval-copy-confirmation.png` | Only the real successful clipboard button; the bearer-link field is outside the capture |
| `extra-work-approval-customer-phone.png` | Independent 390-pixel customer review; fragment removed and no owner notes |
| `extra-work-approval-confirm.png` | Actual final approval confirmation for the fictional scope/amount/timing |
| `extra-work-approval-receipt.png` | Server-confirmed synthetic approval receipt; exact version and timestamp |
| `extra-work-approval-ledger.png` | Owner refresh from D1 showing the same stored synthetic approval |
| `extra-work-approval-declined.png` | Separate server-confirmed synthetic declined branch with its comment |
| `extra-work-approval-changes.png` | Separate server-confirmed synthetic requested-change branch with its comment |

All nine images were visually reviewed for source fidelity and absence of real customer content or management/customer bearer tokens. Public captions must retain the local/synthetic distinction. The copy button is an auxiliary storyboard asset; its small size is intentional. Representative browser-produced PDFs are kept in ignored `.local/qa/` for release visual inspection, not added to the public gallery.

Capture original UI after the implementation is available. Use only fresh fictional requests; remove or close link-display and recovery panels before capture. Keep address-bar fragments, capabilities, session state, real customer content, private owner notes and infrastructure identifiers out of the image. Prefer a browser viewport capture without chrome. Revoking a token is not a reason to publish its value.

Do not generate a fake screenshot with an image generator. Do not claim local images came from a deployed app. If sanitization is required, produce and inspect a clearly documented derivative using the appropriate permitted image-editing workflow; preserve original private evidence outside the public repository.

Copy only cleared images to the portfolio's `public/images/` in the separate integration run. The schema requires `/images/<safe-name>.png` (or jpg/jpeg/webp/avif), descriptive alt text of at least ten characters, and a caption of at least ten characters. The portfolio helper also requires the file to exist before insertion.
