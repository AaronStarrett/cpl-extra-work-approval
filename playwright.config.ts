import {defineConfig, devices} from '@playwright/test';
export default defineConfig({testDir:'tests/browser',fullyParallel:false,workers:1,retries:0,timeout:45000,reporter:[['list']],use:{baseURL:'http://localhost:4174',trace:'off',screenshot:'off'},projects:[{name:'chromium',use:{...devices['Desktop Chrome']}}],webServer:{command:'wrangler dev --env local --ip 127.0.0.1 --port 4174 --persist-to .local/browser-state',url:'http://localhost:4174/api/config',reuseExistingServer:false,timeout:120000}});
